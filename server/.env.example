# Server environment variables (create a local .env file or set them in your host dashboard)
# Port the Express server will listen on (optional)
PORT=4000
# How often the server refreshes external feeds (milliseconds)
REFRESH_INTERVAL_MS=300000
# Comma-separated allowed origins for CORS (set to your frontend URL(s))
ALLOWED_ORIGINS=http://localhost:3000
# Alternate WIND_BASE_URL if you want to override the default Windborne feed URL
WIND_BASE_URL=https://a.windbornesystems.com/treasure